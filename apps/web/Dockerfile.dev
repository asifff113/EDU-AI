# syntax=docker/dockerfile:1
FROM node:20-alpine

# Pin pnpm to the same version used in the monorepo
RUN corepack prepare pnpm@10.15.0 --activate
WORKDIR /workspace

EXPOSE 3000

CMD ["pnpm", "dev"]


